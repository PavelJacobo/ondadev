import { EventsmodelDirective } from './eventsmodel.directive';

describe('EventsmodelDirective', () => {
  it('should create an instance', () => {
    const directive = new EventsmodelDirective();
    expect(directive).toBeTruthy();
  });
});
